import TopAppBar from '../features/controls/components/TopAppBar';
import Modal from '../features/modal/components/Modal';

const EmployeesPage = () => {
  return (
    <div>
      <TopAppBar />
      <Modal title="Сортировка">Тут будет сортировка</Modal>
    </div>
  );
};

export default EmployeesPage;
