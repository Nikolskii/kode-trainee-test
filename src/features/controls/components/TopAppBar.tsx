import Title from '../../../components/Title';
import SearchBar from './SearchBar';

const TopAppBar = () => {
  return (
    <div>
      <Title>Поиск</Title>
      <SearchBar />
    </div>
  );
};

export default TopAppBar;
