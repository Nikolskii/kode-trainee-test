import styled from 'styled-components';

const StyledTopAppBar = styled.section`
  padding: 16px 16px 0;
  border-bottom: 0.3px solid var(--colors-rest);
  display: flex;
  flex-direction: column;
  row-gap: 17px;
`;

export default StyledTopAppBar;
