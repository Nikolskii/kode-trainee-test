import styled from 'styled-components';

export const ModalTitle = styled.h2`
  font-weight: 600;
  font-size: 20px;
  line-height: 24px;
  color: var(--text-color-primary);
  margin: 0;
  text-align: center;
`;
