import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
  justify-content: space-between;
  border-bottom: 0.5px solid var(--colors-rest-secondary);
`;

export default Wrapper;
