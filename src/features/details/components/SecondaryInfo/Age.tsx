import styled from 'styled-components';

const Age = styled.p`
  font-weight: 500;
  font-size: 16px;
  line-height: 20px;
  color: var(--text-color-secondary-light);
  margin: 0;
  padding: 22px 0;
`;

export default Age;
