import styled from 'styled-components';

const Tag = styled.span`
  font-weight: 400;
  font-size: 17px;
  line-height: 22px;
  color: var(--text-color-secondary-light);
  padding-left: 4px;
`;

export default Tag;
