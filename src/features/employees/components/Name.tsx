import styled from 'styled-components';

const Name = styled.p`
  margin: 0;
  font-weight: 500;
  font-size: 16px;
  line-height: 20px;
  color: var(--text-color-primary);
  align-self: end;
  grid-column: 2/3;
  grid-row: 1/2;
`;

export default Name;
